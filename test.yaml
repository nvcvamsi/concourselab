- name: Create namespace in target Kubernetes cluster
  kubernetes.core.k8s:
    state: present
    kubeconfig: "/tmp/config"
    kind: Namespace
    name: kube-system
    definition:
      metadata:
        labels:
          backuptype: bkp
